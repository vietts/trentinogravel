# macOS
.DS_Store
._*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Testing artifacts
.playwright-mcp/
tg-*.jpeg
/*.jpeg
/*.png.backup

# Node / build
node_modules/
dist/
.cache/

# Photos are managed via PageCMS (committed to git, served by Cloudflare)
# photos/  ← removed: now tracked in git

# Large binary assets that don't belong in git
*.pdf

# Worktrees
.worktrees/

# Logs / env
*.log
.env
.env.local

# Raw PDF extraction dump (not shipped) — only keep referenced logos
extracted/*.tif
extracted/img-*.png
extracted/page*.svg
extracted/logo-icon.svg
extracted/logo-illustration-only.svg
extracted/logo-png-extracted.png
extracted/logo-trentino-gravel.svg
extracted/brand-*.png

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
